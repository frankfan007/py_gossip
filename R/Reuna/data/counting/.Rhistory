source("../../../util/util.r")
library(ggplot2)
library(reshape)
reuna.raw <- read_node_raw()
reuna.raw
reuna.raw_plot <- plot_raw(reuna.raw_plot, title="Reuna counting experiment")
reuna.raw_plot <- plot_raw(reuna.raw, title="Reuna counting experiment")
png("reuna_counting_exp.png",width=800,height=600)
reuna.raw_plot
dev.off()
reuna.raw_plot
getwd()
png("reuna_counting_exp.png",width=800,height=600)
reuna.raw_plot
dev.off()
q()
